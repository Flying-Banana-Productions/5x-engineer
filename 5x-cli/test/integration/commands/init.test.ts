/**
 * Integration tests for `5x init` — CLI stdout/stderr/exit-code assertions.
 *
 * Tests that assert on filesystem side effects have been converted to unit
 * tests in test/unit/commands/init.test.ts (Phase 4).
 *
 * This file is intentionally empty — all init tests were convertible to
 * direct-call unit tests. The file is retained as a placeholder for future
 * integration tests that require CLI-layer assertions.
 */
